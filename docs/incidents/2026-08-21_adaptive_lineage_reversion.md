# Adaptive production lineage incident record

## Classification

`ACCIDENTAL_CONFIGURATION_REVERSION`

## Accepted scientific identity

The accepted post-smoke-test satellite-to-satellite topology profile was:

- `isl_policy = grid_adaptive`
- `adjacent_search_k = 1`
- `max_inter_plane_links_per_sat = 1`

The other accepted identity fields remain unchanged: 10 minutes, 60-second
steps with inclusive `t=0..10` (11 timesteps), J2000 epoch, SGP4 propagation,
phasing factor 1, 10,000 km maximum ISL distance, and
`persistent_temporal_union_edges_v1` failure semantics.

## Accidental reversion

The integrated-ground pilot/design configuration reintroduced a stale
`grid_fixed` default. The reversion was introduced by pilot commit
`f6049e989...` and propagated into the final 10K contract and downstream
lineage.

## Affected lineage

`final 10K -> RF/TGNN -> held-out -> external -> DSS`

## Evidence handling

The existing fixed-policy contract, training roots, release tags, and all
fixed-policy results remain historical evidence and are not overwritten,
relabeled, moved, or regenerated by Phase 0. The corrected branch uses the
new lineage identity `final_integrated_dataset_10k_adaptive_v2` and requires
explicit propagation of the adaptive profile at production boundaries.
